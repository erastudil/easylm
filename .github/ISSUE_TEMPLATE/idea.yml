name: Idea
description: A change that is not a bug
title: "[idea] "
labels: ["idea"]
body:
  - type: textarea
    id: want
    attributes:
      label: What you want
      description: The job the app should do. One scene, not a roadmap.
    validations:
      required: true
  - type: textarea
    id: now
    attributes:
      label: What it does now
    validations:
      required: false
